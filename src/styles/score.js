import styled from 'styled-components';

const ScoreSection = styled.section`
    button {
        font-size: 20px;
        border-radius: 40%;
        margin-top: 40px;
        height: 2rem;
    }

    h1, h3 {
        padding: 0.4rem;
    }
`;

export default ScoreSection;
